# 本機對照紀錄

2026-09-07，Windows 開發環境、IPv4。**不是 Hostinger 測試，也不代表海外機房結果。**

| 項目 | 結果 | HTTP 路徑 | 補充 |
|---|---|---|---|
| 數據平台首頁 | 可連線 | 200 | HTML 對照 |
| NewsHomeList 直接 GET | 收到 HTML，非 JSON | 302 → 302 → 200 | 單看最終 200 會誤判 |
| NewsHomeList 正常首頁工作階段 | 有效 JSON | 首頁 200 → 介面 200 | 本次為空陣列 0 筆，證明可解析，不代表已有業務資料 |
| 縣府公所 JSON 檔案 | 有效 JSON | 200 | 13 筆 |
| 縣府開放資料專區 | 可連線 | 302 → 200 | HTML 對照 |

程式保留 TLS 憑證驗證；沒有偽造來源國家或繞過存取控制。工作階段版沿用公開首頁自身的防偽欄位及 Cookie。平台可能更動介面，這份結果只作為同端點重測的比較基準。

部署到 Hostinger 後，使用相同 IPv4 模式執行全部預設並下載 JSON，再依 DNS、TLS、重新導向、HTTP 與 JSON 結果逐項比較。

{
  "name": "shinzhu-hostinger-api-probe",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side connectivity experiment for Hsinchu County data services",
  "main": "server.js",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.js",
    "build": "node --check server.js && node --check api-probe/probe.js && node --check api-probe/public/client.js",
    "test": "node --test tests/*.test.cjs"
  }
}

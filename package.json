{
  "name": "shinzhu-destruction",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "我的反派學院：超認真毀滅新竹計畫",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "vue-tsc --noEmit",
    "probe:build": "node --check api-probe/probe.js && node --check api-probe/public/client.js && node --check server.js",
    "probe:test": "node --test tests/*.test.cjs"
  },
  "dependencies": {
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "@types/node": "^24.0.0",
    "laravel-vite-plugin": "^3.2.0",
    "typescript": "^5.9.3",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11"
  }
}
